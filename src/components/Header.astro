---
interface Props {
  title?: string;
}

const { title } = Astro.props as Props;
---

<header class="p-4 text-center">
  <h1 class="text-3xl">{title || "Hacktiv8 CV's and Portfolios"}</h1>
</header>
